= render 'layouts/head'

%body{ :class => controller_name, :id => action_name }

  %header
    %h1= t 'name'
    
  %section
    = yield

  %footer
    = raw(t 'footer.copyright')
    %span &mdash;
    = mail_to("chris@chocol.it", "Contact", :encode => "hex")
    %span &mdash;
    = link_to((t 'footer.login_btn'), request_path)
