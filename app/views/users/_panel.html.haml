.overlay.active
  .panel
    %h2= t 'panel.title', :name => current_user.name
    %span= t 'panel.body'

    = form_for(@user) do |f|
      = f.email_field :email, :autofocus => true, :placeholder => 'your@email.com'
      = f.submit (t 'home.request_btn')
